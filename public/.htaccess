Options -Indexes

<IfModule mod_rewrite.c>
    RewriteEngine On

    # 🟢 تم إزالة الـ RewriteBase القديم لأن الصب دومين هو الجذر الآن
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [L]
</IfModule>